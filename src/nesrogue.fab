mode main()
    // Set the palette:
    palette = example_palette
    ppu_upload_palette()

    // Loop forever:
    while true

// Define the tileset (commonly called CHR):
chrrom
    file(fmt, "tiles.png")
        